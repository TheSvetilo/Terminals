package com.vbogd.terminals.data.terminalRepository

import com.vbogd.terminals.domain.TerminalRepository
import com.vbogd.terminals.domain.model.Terminal

class TerminalRepository : TerminalRepository {

    override fun getTerminals(): List<Terminal> {
        TODO("Not yet implemented")
    }

    override fun getTerminalById(id: Int): Terminal? {
        TODO("Not yet implemented")
    }
}